# Connection string used by both FastAPI and the fastMCP server.
DATABASE_URL=postgresql+psycopg2://mcp_user:mcp_password@localhost:5432/mcp_store
# Set to 1 to enable verbose SQL logging.
SQLALCHEMY_ECHO=0
